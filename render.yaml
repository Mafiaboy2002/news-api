services:
  - type: web
    name: news-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free

cronjobs:
  - name: scrape-news
    schedule: "hourly"
    command: "python scraper.py"
